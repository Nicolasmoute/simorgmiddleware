import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The proxy routes are fully dynamic and run on the Node.js runtime.
  reactStrictMode: true,
};

export default nextConfig;
